*** Update File: src/components/Navbar.tsx
@@
-export function Navbar() {
-  const [open, setOpen] = useState(false);
-  const [hidden, setHidden] = useState(false);
-  const [shrink, setShrink] = useState(false);
-  const [active, setActive] = useState<string>('');
+export function Navbar() {
+  const [open, setOpen] = useState(false);
+  const [active, setActive] = useState<string>('');
   const pathname = usePathname();
-  const lastY = useRef(0);
 
   useEffect(() => {
     setOpen(false);
   }, [pathname]);
 
-  // Hide on scroll down, show on scroll up. Shrink after slight scroll.
-  useEffect(() => {
-    function onScroll() {
-      const y = window.scrollY;
-      setShrink(y > 12);
-      if (y > lastY.current + 5 && y > 64) setHidden(true);
-      if (y < lastY.current - 5) setHidden(false);
-      lastY.current = y;
-    }
-    window.addEventListener('scroll', onScroll, { passive: true });
-    onScroll();
-    return () => window.removeEventListener('scroll', onScroll);
-  }, []);
-
   // Scrollspy for in-page sections on home route
   useEffect(() => {
     if (pathname !== '/') return;
-    const ids = ['services', 'process', 'case-studies', 'testimonials'];
+    const ids = ['services', 'process', 'about'];
     const els = ids
       .map((id) => document.getElementById(id))
       .filter((el): el is HTMLElement => Boolean(el));
@@
-  return (
-    <header className={site-header transition-transform duration-300 }>
-      <div className={container flex items-center justify-between  transition-all}>
+  return (
+    <header className="site-header">
+      <div className="container flex h-16 items-center justify-between">
         <Link className="logo" href="/" aria-label="Zenthara Home">
           <span className="logo-mark" aria-hidden>?</span>
           <span className="logo-text">Zenthara</span>
         </Link>
-          <button
+        <button
           className="nav-toggle"
           aria-label="Toggle navigation"
           aria-expanded={open}
@@
-      {open && (
-        <div className="md:hidden border-t border-gray-200 bg-white">
+      {open && (
+        <div className="md:hidden border-t border-gray-200 bg-white">
           <div className="container py-3 flex flex-col gap-3">
             <Link href="/">Home</Link>
             <Link href="/#services">Services</Link>
             <Link href="/#process">Process</Link>
             <Link href="/portfolio">Portfolio</Link>
             <button onClick={() => openContactModal()} className="btn btn-primary text-left" type="button">Contact</button>
           </div>
         </div>
       )}
     </header>
   );
 }
*** End Patch
